global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "trading"
    static_configs:
      - targets: ["trading:9090"]
    scrape_interval: 5s
