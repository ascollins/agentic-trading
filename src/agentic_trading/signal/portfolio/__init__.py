"""Portfolio management — canonical location: ``agentic_trading.signal.portfolio``.

Previously at ``agentic_trading.portfolio``.

Modules:
    manager.py          PortfolioManager — aggregates signals into targets
    sizing.py           Position sizing methods
    allocator.py        Cross-strategy allocation
    intent_converter.py TargetPosition → OrderIntent bridge
    correlation_risk.py Correlation-based risk analysis
"""
