"""Domain layer — canonical events, value objects, aggregates.

This package defines the bounded-context primitives that every other
layer depends on but never modifies.  Everything here is immutable.
"""
